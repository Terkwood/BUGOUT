#!/bin/bash

# Invoke this if you'd prefer to just sit around,
# and do nothing, instead of actually turning on the
# gateway when the container boots.

# Useful when you're iterating on a branch.

tail -f /dev/null